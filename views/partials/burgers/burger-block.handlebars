<li>
    <h5> {{this.id}} - {{this.burger_name}} </h5>
    <button type="button" class="eat-burger btn btn-secondary" data-id="{{this.id}}"
        data-newDevoured="{{this.devoured}}">Eat</button>
    
</li>